
services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ruki-v-dom
    restart: always
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    # Если нужны переменные окружения, раскомментируйте и добавьте путь к файлу
    # env_file:
    #   - .env.production
